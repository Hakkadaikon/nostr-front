import type { Tag } from '../../lib/nostr/nip10';
import { buildReplyTags } from '../../lib/nostr/nip10';
import { buildRepostTags } from '../../lib/nostr/nip18';

export { buildReplyTags, buildRepostTags };
